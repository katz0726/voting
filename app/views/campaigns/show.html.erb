<div class="row">
  <h1 class="page-title"><%= @campaign.title %></h1>

  <div class="col-md-8">
    <p>
      発信者: <%= format_username(@campaign.user.username_last, @campaign.user.username_first) %>&ensp;
      宛先: <%= @campaign.campaign_for %>
    </p>

    <div><%= @campaign.content %></div>

    <div class="comment-area">
      <h3>賛同者からのコメント</h3>
      <% @comments.each do |comment| %>
        <div class="comment-area-inner">
          <p><strong><%= format_username(comment.user.username_last, comment.user.username_first) %></strong> <%= comment.updated_at.strftime("%Y-%m-%d") %></p>
          <div><%= comment.content %></div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="col-md-4">
    <div>
      <strong><%= delimited_by_comma(@campaign.supporters.count) %>人が賛同しました。</strong>もう少しで
      <%= delimited_by_comma(@campaign.goal) %>人に到達します！

    </div><hr/>
  </div>
</div>