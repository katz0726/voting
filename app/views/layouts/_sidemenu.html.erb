<div class="col-md-3 float-right campaign-topic-area">
  <h3>トピック</h3>

  <% @tags.each do |tag| %>
  <%= link_to tag.name, campaign_path(tag), {class: 'btn btn-primary campaign-topic-link', roll: 'button'}%>
  <% end %>
</div>